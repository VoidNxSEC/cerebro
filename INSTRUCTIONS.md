# What we need to do ? 

-. We need to switch poetry2nix for poetry, for avoid .venv
-. We need to assign the elasticsearch_5.py file with sinergy that match retriever.py file, so, for support heavy workloads, we need to change the data base for elastic search, that is the rationale

For now, that is.
