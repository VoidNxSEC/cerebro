name = "cerebro-docs"
compatibility_date = "2025-01-01"

# Static site served via Cloudflare Pages
# Deploy with: wrangler pages deploy site/ --project-name "$CLOUDFLARE_PAGES_PROJECT"
#
# Set `CLOUDFLARE_PAGES_PROJECT` to the real Pages project for this workspace.
